name: Build Windows EXE

on:
  push:
    branches: [ main ]
    paths:
      - 'server.py'
      - 'core/**'
      - 'dashboard/**'
      - 'build_win.spec'
      - 'requirements.txt'
      - '.github/workflows/build-windows.yml'
  workflow_dispatch: {}

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build with PyInstaller
        run: pyinstaller build_win.spec --noconfirm --clean

      - name: Zip the build output
        run: |
          cd dist
          Compress-Archive -Path BybitEmaDashboard -DestinationPath BybitEmaDashboard-Windows.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: BybitEmaDashboard-Windows
          path: dist/BybitEmaDashboard-Windows.zip
          retention-days: 30
